import React from 'react';

import PageNotFound from '../Images/PageNotFound.gif';

const NotFound = () => {
  return <img src={PageNotFound} style={{ marginTop: '5rem' }} />;
};

export default NotFound;
